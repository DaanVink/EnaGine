void test() {
//    if ((dir = opendir (cwd)) != NULL) {
//      /* print all the files and directories within directory */
//      while ((ent = readdir (dir)) != NULL) {
//        printf ("%s\n", ent->d_name);
//        strcat(filesInDirectory, ent->d_name);
//        strcat(filesInDirectory, "|");
//      }
//      closedir (dir);
//    }
}
